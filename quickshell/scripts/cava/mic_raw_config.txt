# Mic-input cava for the notch dictation waveform (services/Hyprvoice.qml).
# @DEFAULT_SOURCE@ is libpulse's magic name for the current default mic.
[general]
mode = waves
framerate = 60
autosens = 1
bars = 18

[input]
method = pulse
source = @DEFAULT_SOURCE@

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
channels = mono
mono_option = average

[smoothing]
noise_reduction = 12
