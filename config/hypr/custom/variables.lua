-- User variable overrides (loaded after hyprland/variables.lua, before keybinds). Added 2026-06-07.

-- Make Super+E open our Yazi "Finder" instead of Dolphin (single bind, no double-fire).
fileManager = "$HOME/.config/hypr/custom/scripts/finder.sh"
